import React from "react";
import { HomeContainer, Title } from "./home.style";

const HomeScreen = () => {
    return (
        <HomeContainer>
            <Title>
                HomePage
            </Title>
        </HomeContainer>
    );
};

export default HomeScreen;
